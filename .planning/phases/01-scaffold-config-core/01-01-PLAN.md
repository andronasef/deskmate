---
phase: 01-scaffold-config-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, index.html, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, .prettierrc, .prettierignore, .gitignore, src/main.tsx, src/index.css, src/app/App.tsx, src/app/App.module.css, src/app/App.test.tsx, src/vite-env.d.ts, src/test/setup.ts]
autonomous: false
requirements: [CFG-05]
user_setup: []

must_haves:
  truths:
    - "User opens the app URL (npm run dev) and sees the dark dashboard shell: 'DeskMate' wordmark header + placeholder card"
    - "The project builds, type-checks, and lints clean with the pinned toolchain (Vite 8.2, React 19.2, TypeScript ~6.0.3 — NOT 7)"
    - "The dark theme token set is declared on :root (--background #0A0D12, --surface #141A22, --accent #22D3EE, ...)"
    - "vitest runs a colocated App.test.tsx asserting the exact UI-SPEC copy"
  artifacts:
    - path: "package.json"
      provides: "pinned dependency manifest"
      contains: "typescript ~6.0.3, react 19.2, vite 8.2, zustand 5, zod 4"
    - path: "src/app/App.tsx"
      provides: "dashboard shell render"
      exports: ["App"]
    - path: "src/index.css"
      provides: "dark theme tokens on :root"
      contains: "--background: #0A0D12"
    - path: "src/app/App.test.tsx"
      provides: "shell copy + structure assertions"
    - path: "eslint.config.js"
      provides: "flat-config lint gate"
    - path: "vite.config.ts"
      provides: "build + vitest (jsdom) harness"
  key_links:
    - from: "src/main.tsx"
      to: "src/app/App.tsx"
      via: "ReactDOM.createRoot().render(<App/>)"
      pattern: "render.*App"
    - from: "src/main.tsx"
      to: "@fontsource-variable/inter"
      via: "font import"
    - from: "src/app/App.tsx"
      to: "src/app/App.module.css"
      via: "className imports"
---

<objective>
Scaffold DeskMate as a bootable Vite 8 + React 19 + TypeScript ~6.0.3 static app with a green lint/type/test toolchain, and render the Phase 1 dark dashboard shell (header + placeholder card) exactly per 01-UI-SPEC.md.

Purpose: The walking skeleton's boot path — a real user opens the app URL and sees the DeskMate shell. CFG-05's "never bricks" contract is seeded here (the boot path exists) and fully implemented in plan 01-02. All Phase 1 dependencies install in this plan so 01-02 is pure code.

Output: A static app that runs with `npm run dev`, builds with `npm run build`, and renders the UI-SPEC shell; a pinned package manifest every later phase builds on.
</objective>

## Phase Goal

**As a** user opening DeskMate on any device, **I want to** boot straight into a working dashboard shell loaded through one validated config path that never breaks on corrupt or mismatched stored data, **so that** I always get a working dashboard and my original config is never silently lost.

> Note: the ROADMAP `**Goal:**` line is outcome-shaped but not in strict "As a / I want to / so that" form. This story is derived verbatim from ROADMAP Phase 1 success criteria 1–3; run `/gsd mvp-phase 1` if you want the ROADMAP line canonicalized.

<execution_context>
@C:/Users/A/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/A/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-config-core/01-CONTEXT.md
@.planning/phases/01-scaffold-config-core/01-UI-SPEC.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts this plan produces and downstream plans consume. -->

UI-SPEC shell token set (declare on :root in src/index.css — exact values):
```css
--background:    #0A0D12
--surface:       #141A22
--surface-raised:#141A22
--accent:        #22D3EE
--destructive:   #F87171   /* declared, NOT used in Phase 1 */
--text-primary:  #E6EAF0
--text-muted:    #8A93A0
--border:        #232B36
```

UI-SPEC typography roles (Phase 1 usage): Heading 20px/600/1.2 (wordmark + placeholder heading), Body 16px/400/1.5 (placeholder body), Label 14px/400/1.5 (declared only). Font: Inter Variable via `@fontsource-variable/inter` (self-hosted, no network dependency).

UI-SPEC spacing scale (multiples of 4): xs 4, sm 8, md 16, lg 24, xl 32, 2xl 48 (header height), 3xl 64.

Exact copy (UI-SPEC Copywriting Contract):
- Wordmark: "DeskMate"
- Placeholder heading: "Your dashboard is ready"
- Placeholder body: "Widgets will render here — grid editing arrives in the next update."
- Console warning format (the only error surface, per D-07): `[deskmate:storage] config rejected (version=…): <reason> — raw backup preserved at deskmate.config.backup`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the Phase 1 dependency set (package legitimacy gate)</name>
  <read_first>STACK.md (versions verified against the npm registry 2026-08-07)</read_first>
  <acceptance_criteria>
    - Human approves every package below after npmjs.com verification; no [SLOP] package may be approved
  </acceptance_criteria>
  <files>package.json, package-lock.json (dependency manifest this gate approves before Task 2 installs it)</files>
  <action>
    Human action required (blocking gate — never auto-approvable): review the [ASSUMED] dependency set below on npmjs.com and approve or reject before any install runs. Open each URL listed in &lt;how-to-verify&gt;, confirm the package and pinned version exist with the expected publisher, then reply "approved" (or list packages to replace).
  </action>
  <verify>Human explicitly replies "approved" with no packages rejected; the resume signal is received</verify>
  <done>All [ASSUMED] packages verified on npmjs.com and approved by the human; Task 2 install may proceed</done>
  <what-built>Nothing yet — this is a pre-install blocking gate. All packages below are status [ASSUMED] (no Package Legitimacy Audit table exists in research/, so the fallback policy applies). Verify each on npmjs.com before install proceeds.</what-built>
  <how-to-verify>
    1. For each package below, open the npmjs.com/package URL and confirm: (a) the exact package exists, (b) the pinned version exists, (c) the publisher is the known maintainer org (typosquat check — exact name, no lookalike characters):
       - react / react-dom @ 19.2.8 → npmjs.com/package/react (publisher: facebook)
       - vite @ 8.2.1 → npmjs.com/package/vite (publisher: voidzero)
       - @vitejs/plugin-react @ 6.0.5 → npmjs.com/package/@vitejs/plugin-react
       - typescript @ ~6.0.3 (NOT 7.0.2 — typescript-eslint peer is >=4.8.4 <6.1.0) → npmjs.com/package/typescript
       - typescript-eslint @ 8.66.x → npmjs.com/package/typescript-eslint
       - eslint @ 10.8.x → npmjs.com/package/eslint
       - eslint-plugin-react-hooks, eslint-plugin-react-refresh, globals, @eslint/js (template defaults) → npmjs.com/package/eslint-plugin-react-hooks
       - prettier @ 3.9.x + eslint-config-prettier → npmjs.com/package/prettier
       - zustand @ 5.0.14 → npmjs.com/package/zustand (publisher: pmndrs)
       - zod @ 4.4.3 → npmjs.com/package/zod
       - @fontsource-variable/inter @ 5.3.0 → npmjs.com/package/@fontsource-variable/inter
       - vitest @ latest 4.x → npmjs.com/package/vitest (confirm peer range accepts vite ^8)
       - jsdom, @testing-library/react, @testing-library/jest-dom → npmjs.com/package/@testing-library/react (peer react ^19)
    2. Confirm no listed name is a typo-squat lookalike of a package you recognize.
  </how-to-verify>
  <resume-signal>approved — or list any package to reject/replace before install</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Vite 8 + React 19 + TypeScript ~6.0.3 with lint/type/test toolchain</name>
  <files>package.json, package-lock.json, index.html, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, .prettierrc, .prettierignore, .gitignore, src/vite-env.d.ts, src/test/setup.ts, src/main.tsx</files>
  <read_first>STACK.md (pinned versions + install commands), 01-CONTEXT.md (D-12 vitest), ARCHITECTURE.md (recommended src/ structure)</read_first>
  <acceptance_criteria>
    - `node -p "require('./package.json').devDependencies.typescript"` prints `~6.0.3` (never 7.x)
    - `npm run typecheck` exits 0 (tsc --noEmit)
    - `npm run lint` exits 0 (eslint flat config)
    - `npm run build` exits 0 and produces dist/
    - `npm test` exits 0 (vitest run, passWithNoTests until Task 3 adds tests)
    - package.json contains "type": "module" and scripts: dev, build, preview, lint, typecheck, test
    - No scaffold cruft remains (src/App.css, src/assets/react.svg removed)
  </acceptance_criteria>
  <action>
    Scaffold the canonical baseline per STACK.md:
    1. Run `npm create vite@latest . -- --template react-ts` in a temporary subdirectory (e.g. `.scaffold-tmp/`) because the repo root is non-empty (.planning/ exists), then move the generated files (package.json, package-lock.json, index.html, tsconfig*.json, vite.config.ts, eslint.config.js, .gitignore, src/) into the repo root and delete the temp directory. Do NOT move or overwrite anything under .planning/.
    2. Pin TypeScript: edit package.json devDependencies to `"typescript": "~6.0.3"` — the create-vite template scaffolds TS 7, which violates typescript-eslint@8.66's peer range (`>=4.8.4 <6.1.0`) and breaks the lint toolchain. Do not use TS 7.0.2.
    3. `npm install`, then `npm install zustand@5.0.14 zod@4.4.3 @fontsource-variable/inter@5.3.0`, then `npm install -D vitest jsdom @testing-library/react @testing-library/jest-dom prettier eslint-config-prettier`. (react, react-dom, vite, @vitejs/plugin-react, eslint, typescript-eslint, eslint-plugin-react-hooks, eslint-plugin-react-refresh, globals, @eslint/js come from the template.)
    4. package.json: ensure `"type": "module"` (Vite 8 is ESM-only). Scripts: `dev: "vite"`, `build: "tsc -b && vite build"`, `preview: "vite preview"`, `lint: "eslint ."`, `typecheck: "tsc -b --noEmit"`, `test: "vitest run"`, `test:watch: "vitest"`.
    5. vite.config.ts: keep the react() plugin from @vitejs/plugin-react; add a vitest `test` block by importing `defineConfig` from `vitest/config` with `{ environment: 'jsdom', globals: true, setupFiles: ['./src/test/setup.ts'], passWithNoTests: true }`. Do NOT enable the React Compiler preset (per STACK.md — not needed for v1).
    6. Create `src/test/setup.ts` containing exactly: `import '@testing-library/jest-dom/vitest';`
    7. eslint.config.js: flat config — typescript-eslint recommended + react-hooks + react-refresh + `eslint-config-prettier` listed LAST (prettier wins on formatting rules). This pins the toolchain shape typescript-eslint@8.66 supports.
    8. .prettierrc: `{ "semi": true, "singleQuote": true, "printWidth": 100 }` (agent discretion). Add .prettierignore with `dist`, `node_modules`, `.planning`.
    9. tsconfig: keep the template's strict settings (`"strict": true`, `noUnusedLocals`, `noUnusedParameters`, moduleResolution `"bundler"`, `jsx: "react-jsx"`, target ES2022). tsconfig.app.json includes `src`; tsconfig.node.json covers vite.config.ts.
    10. index.html: set `<title>DeskMate</title>`, `lang="en"`, keep the viewport meta.
    11. Remove template cruft: `src/App.css`, `src/assets/react.svg`, and the template App.tsx content (replaced in Task 3). Replace `src/main.tsx` with: imports for `@fontsource-variable/inter`, `./index.css`, `App`, and `createRoot(...).render(<StrictMode><App /></StrictMode>)` into #root.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm test</automated>
  </verify>
  <done>Scaffold boots (npm run dev serves), toolchain all-green, TypeScript pinned ~6.0.3, zustand/zod/inter/vitest/testing-library installed, package-lock.json committed.</done>
</task>

<task type="auto">
  <name>Task 3: Render the Phase 1 dashboard shell per UI-SPEC (header + placeholder)</name>
  <files>src/index.css, src/app/App.tsx, src/app/App.module.css, src/app/App.test.tsx, src/main.tsx</files>
  <read_first>01-UI-SPEC.md (shell contract, tokens, spacing, typography, copy), 01-CONTEXT.md (D-10 no grid/no icons/no error UI, D-14 colocated tests)</read_first>
  <acceptance_criteria>
    - App.test.tsx passes: renders wordmark "DeskMate", heading "Your dashboard is ready", body "Widgets will render here — grid editing arrives in the next update."
    - `getByRole('button')` in App.test.tsx throws (no interactive elements — UI-SPEC: zero actions in Phase 1)
    - src/index.css declares all 8 tokens on :root with the exact UI-SPEC hex values
    - grep gate: `grep -rn "dangerouslySetInnerHTML" src/` returns 0 matches
    - Header is 48px with a 1px bottom hairline (--border); placeholder card uses --surface-raised, 1px --border, 8px radius, 24px internal padding
  </acceptance_criteria>
  <action>
    Build the two shell surfaces exactly per 01-UI-SPEC.md. Do not add a grid, widgets, theme system, icons, buttons, or error UI (D-10; UI-SPEC "Phase 1 scope").
    1. `src/index.css` — global stylesheet: declare the 8 dark tokens on `:root` with the exact hex values from the <interfaces> block above (--background, --surface, --surface-raised, --accent, --destructive, --text-primary, --text-muted, --border); declare the spacing scale as custom properties (--space-xs 4px, --space-sm 8px, --space-md 16px, --space-lg 24px, --space-xl 32px, --space-2xl 48px, --space-3xl 64px); base reset (margin 0, box-sizing border-box); body: font-family `'Inter Variable', system-ui, sans-serif`, background var(--background), color var(--text-primary), line-height 1.5.
    2. `src/app/App.tsx` — functional component `App` exporting nothing else. Renders: a `<header>` with `className={styles.header}` containing the wordmark text "DeskMate" (Heading 20px/600/1.2, --text-primary, left-aligned at 16px padding); a `<main>` with `className={styles.main}` containing a centered card (`className={styles.placeholder}`) with heading "Your dashboard is ready" (Heading 20px/600, --text-primary) and body "Widgets will render here — grid editing arrives in the next update." (Body 16px/400, --text-muted).
    3. `src/app/App.module.css` (CSS Modules per STACK.md — no component library): `.header` = height 48px, width 100%, background var(--surface), border-bottom 1px solid var(--border), padding 0 var(--space-md), display flex, align-items center; `.main` = flex 1, display flex, align-items center, justify-content center, padding var(--space-lg); `.placeholder` = background var(--surface-raised), border 1px solid var(--border), border-radius 8px, padding var(--space-lg), max-width 420px, text-align center.
    4. `src/main.tsx` — import `'@fontsource-variable/inter'` (self-hosted font, no Google Fonts) and `./index.css`, then mount `<StrictMode><App /></StrictMode>` into `#root` with `createRoot`.
    5. `src/app/App.test.tsx` — colocated vitest + Testing Library test (D-14): `render(<App />)`; `screen.getByText('DeskMate')`; `screen.getByText('Your dashboard is ready')`; `screen.getByText('Widgets will render here — grid editing arrives in the next update.')`; `expect(screen.queryByRole('button')).not.toBeInTheDocument()`.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>Shell renders exactly per UI-SPEC; a user running `npm run dev` sees the DeskMate wordmark header and the placeholder card; automated shell test green.</done>
</task>

</tasks>

## Artifacts This Plan Produces

Every symbol this plan creates (for drift-verification exclusion):

- **Files:** package.json, package-lock.json, index.html, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, eslint.config.js, .prettierrc, .prettierignore, .gitignore, src/vite-env.d.ts, src/test/setup.ts, src/main.tsx, src/index.css, src/app/App.tsx, src/app/App.module.css, src/app/App.test.tsx
- **Components:** `App` (src/app/App.tsx — no props, renders header + placeholder per UI-SPEC)
- **CSS:** token set on `:root` (8 dark tokens), spacing scale (7 tokens), `.header`, `.main`, `.placeholder` (src/app/App.module.css)
- **Config:** vite.config.ts (react plugin + vitest jsdom block), eslint.config.js (flat config), package.json scripts (dev/build/preview/lint/typecheck/test) and pinned deps (typescript ~6.0.3, react 19.2.8, vite 8.2.1, zustand 5.0.14, zod 4.4.3, @fontsource-variable/inter 5.3.0)
- **Test harness:** vitest (jsdom, globals, setupFiles), src/test/setup.ts (jest-dom matchers), App.test.tsx (3 copy assertions + no-button assertion)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → local install | npm package content crosses into the project dependency tree (untrusted supply chain) |
| dev server → browser | The only runtime surface in this plan is static UI — no data, no user input, no storage access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm installs (all Phase 1 deps) | mitigate | Blocking-human package legitimacy checkpoint (Task 1) verifies every package + pinned version on npmjs.com before install; versions come from STACK.md's npm-registry-verified table (2026-08-07); [SLOP] never approved; exact pinned versions in package.json |
| T-01-02 | Information disclosure | — | accept | No secrets exist in Phase 1: no API keys, no tokens, no user data, no storage access. Nothing to disclose |
| T-01-03 | Spoofing | — | accept | No identity or messaging surface in this plan (static shell only); postMessage/sandbox surface arrives Phase 4 |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint (Task 1, gate blocking-human) for all [ASSUMED] packages — never auto-approvable |

ASVS L1 check: no high-severity threat in this plan's scope (no data layer, no executable input). Supply-chain tampering (T-01-01/T-01-SC) is the only actionable surface and is blocked by the human checkpoint.
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0, dist/ produced
- `npm test` exits 0 (App.test.tsx green)
- `node -p "require('./package.json').devDependencies.typescript"` → `~6.0.3`
- Grep gates: `grep -rn "dangerouslySetInnerHTML" src/` → 0; `grep -rn "localStorage" src/` → 0 (storage intentionally absent until plan 01-02)
- Human checkpoint (Task 1) resolved: approved
</verification>

<success_criteria>
- A real user runs `npm run dev`, opens the URL, and sees the DeskMate dark shell (wordmark + placeholder card) exactly per UI-SPEC
- Toolchain is fully green on a fresh clone (install → typecheck → lint → test → build)
- TypeScript is pinned ~6.0.3 so typescript-eslint@8.66 works; no TS 7 anywhere
- The vitest + Testing Library harness that plan 01-02's config-core tests (D-13) run on is in place
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-config-core/01-01-SUMMARY.md` when done
</output>
